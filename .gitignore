### Root .gitignore (refined)

# Gradle build cache & local state (never commit)
.gradle/
build/
**/build/

# Local machine / environment config
local.properties

# Android Studio / IntelliJ caches & transient files
*.iml
.idea/
!.idea/codeStyles/ # keep shared code style if added later
!.idea/runConfigurations/ # keep run configs if needed

# External native build outputs
.externalNativeBuild/
.cxx/

# Logs & dumps
*.log
hs_err_pid*

# OS artifacts
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Binary / compiled artifacts not meant for version control
*.dex
*.class
*.apk
*.ap_
*.keystore
*.jks

# We intentionally version a single distributable APK in release-artifacts.
# Allow only that path by negating after the global *.apk ignore above.
!release-artifacts/app-debug.apk

# Generated folders
captures/
.navigation/
out/
bin/
gen/
proguard/

# Misc
*.cab
*.msi
*.msm
*.msp
*.lnk

# Version control leftovers
.svn/
